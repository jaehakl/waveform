{
  "name": "nemoforge-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter waveform-web dev",
    "build": "pnpm --filter waveform-web build",
    "start": "pnpm --filter waveform-web start"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "prettier": "^3.0.0"
  }
}

